import React, { FC } from 'react';
import './index.less';

const NotFound: FC = () => {
  return (
    <div className="page not-found">
      <span>页面不存在</span>
    </div>
  );
};

export default NotFound;
